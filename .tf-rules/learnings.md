[chat] Recharge Affinity slots are keyed by data-recharge-slot name in recharge-slot-injector.js (Map) AND by Recharge SDK — only ONE template can bind per slot name. To stack multiple widgets in one slot (e.g. *.sidebar), put them all inside a SINGLE <script type="text/html" data-recharge-slot="..."> template; a second template with the same slot name silently collides and one widget never renders.
[chat] Liquid inside a Recharge slot's <script type="text/html"> template still renders server-side first (it's a Liquid string baked into the DOM before Recharge injects it), so shop.metaobjects.* and other Liquid data resolve normally inside slot markup.
[chat] Metaobject content in snippets: shop.metaobjects.<type>.values is a LAZY DROP — .first and [0] index BOTH return a blank object (empty system.handle, empty fields) even when .values.size is correct. You MUST iterate to read entries: `{% for entry in shop.metaobjects.<type>.values limit: 1 %}`. Inside the loop access fields directly (entry.field_key.value — NOT entry.fields.field_key, which is GraphQL-only and nil in Liquid). Assign hardcoded fallbacks BEFORE the loop and override inside with | default: so the card renders when no entry is published. Confirmed against the did_you_know_subscriptions_footer metaobject.
[chat] Shopify metaobject auto-generated keys use UNDERSCORES: definition type from "Did you know - Subscriptions Footer" became did_you_know_subscriptions_footer, and field keys from "CTA Label"/"CTA URL" became cta_label/cta_url. (Entry handles use hyphens, e.g. did-you-know — different convention from type/field keys.)
[chat] Metaobject definition "Storefronts API access" toggle = storefront PUBLIC_READ, which DOES enable Liquid shop.metaobjects access (not just the Storefront GraphQL API). When it's on and the entry is Active, the entry resolves in Liquid.
[chat] Renaming a metaobject definition in Shopify Admin does NOT change its underlying `type` key — the key is frozen at creation. To get a clean key you must DELETE + recreate the definition (which drops all existing entries). The Articles footer cards are powered by type key `articles_recharge` (display name "Articles - Recharge"). Always confirm the actual `type:` shown under the Name field; the display name is unreliable. Fields: image (Image/File), eyebrow, headline, link_label, link_url.
[chat] To render a REPEATING list of cards from metaobjects (vs a single did-you-know card), iterate `shop.metaobjects.<type>.values limit: N` and use the for-loop `{% else %}` clause for the empty/fallback case — it fires when no entries are published, letting you keep hardcoded fallback cards inline. snippets/recharge-articles-cards.liquid reads up to 3 `articles_recharge` entries this way.
[chat] Metaobject image fields (type "Image (File)") render via `entry.image.value | image_url: width: N | image_tag: ...` — `.value` is the MediaImage drop. Guard with `{% if entry.image.value != blank %}` before image_url.
[chat] Recharge Affinity portal slot page-ids (the prefix before .header/.footer/.sidebar) do NOT always match the UI tab label: overview=Next order, subscriptions=Subscriptions, schedule=Upcoming orders, orders=Previous orders, customer=Addresses+payment, shipping=Addresses, payment_methods=Payment methods, homepage=Homepage. So the Upcoming orders footer slot is `schedule.footer`. Source: docs.getrecharge.com/docs/adding-custom-content-to-affinity.
[chat] FAQ footer (schedule.footer / Upcoming orders) uses a CONTAINER metaobject pattern, not a flat repeating list: type `faq_recharge_footer` has `title` (single_line_text) + `faq` (list.metaobject_reference → `faq_recharge`); each `faq_recharge` item has `question` (single_line_text) + `answer` (multi_line_text). In Liquid, read the container by iterating `shop.metaobjects.faq_recharge_footer.values limit:1`, then iterate the list field `entry.faq.value` (a list of MetaobjectDrops) and access `item.question.value` / `item.answer.value` directly. Render multi-line answers with `| newline_to_br`. snippets/recharge-faq.liquid keeps 4 hardcoded fallback items when no entry is published.
[chat] The Recharge Affinity *.sidebar slot does NOT reach the live portal sidebar via recharge-slot-injector.js — that injector only targets [data-slot]/[data-recharge-slot-target]/[data-rc-slot], but the real portal renders the sidebar as [data-testid="sidebar-container"] (with a Recharge-owned extension div inside). To place custom content (e.g. the Did You Know card) there, inject it manually: APPEND a cloned <template> into [data-testid="sidebar-container"] as a sibling (last child) — never set innerHTML (wipes the extension). Use a MutationObserver (SPA re-renders on tab switch) + an idempotent guard (skip if .cf-did-you-know already present). See snippets/did-you-know-sidebar-inject.liquid.
[chat] Recharge Affinity ".header" slots render BELOW the persistent tab navigation (Next order / Subscriptions / Order history / Upcoming orders / Account). To place content ABOVE the tabs you cannot use a slot — inject manually: text-match the known tab labels among all <a>/<button>, walk up to the first shared parent (the nav row), and insertBefore it. MutationObserver + idempotent guard (skip if target class already present) because the portal is a SPA. See snippets/recharge-alert-inject.liquid. Content placed above the persistent nav is shown on ALL tabs, not tab-scoped.
[chat] Recharge Affinity portal tab nav DOM: tabs are `[role="tab"]` divs inside a `[role="tablist"]`, each with `data-testid="portal-nav-item-<id>_navigation_link"`, `data-key="/<id>"`, and the ACTIVE tab has `aria-selected="true"` (Subscriptions example: data-key="/subscriptions", class recharge-tab-navigation-item). To place content ABOVE the tabs, insertBefore the `[role="tablist"]` in its parent. To make it tab-scoped, read the tab with aria-selected="true" and match its text/data-key/testid; MutationObserver MUST include `attributes:true, attributeFilter:['aria-selected']` (tab switches toggle that attribute, not childList). See snippets/recharge-alert-inject.liquid.
[chat] Contaminant alert (snippets/recharge-alert-schedule.liquid) content is driven by the `alert_recharge` metaobject: fields `heading` (single_line_text) → alert title, `description` (multi_line_text) → alert body (piped through newline_to_br). Iterate `shop.metaobjects.alert_recharge.values limit: 1`, assign hardcoded fallbacks before the loop, override with `| default:` inside. Storefronts API access is ON for this definition.
[chat] cf-tracker-widget-extension.js business logic depends on the Recharge customer-portal SDK global `recharge` (recharge.auth.loginCustomerPortal(), recharge.subscription.listSubscriptions(session,{status:'Active'}), recharge.subscription.updateSubscription(session,id,{properties:[...]})). The SDK is not ready at element connect — poll `typeof recharge !== 'undefined' && recharge.auth` (retry ~20×1s) before using it. Subscription line-item props `_household_size` and `_last_reset_date` drive lifespan; pack size is parsed from the variant title ("3-Pack"/"6 Pack"→3/6, "Single"→1, else 1).
[chat] The pitcher tracker's EWG contaminant feature has TWO unmet external deps in this repo: (1) the EWG API key is only documented as a truncated prefix `5a4227b3-...` — the full key is NOT in the repo, set as const EWG_API_KEY (or window.theme.contaminantSettings.ewgApiKey); (2) window.theme.contaminantSettings.contaminants (the filter's removal list with name + percentRemoved) is NOT defined anywhere. Until both are provided the EWG step fails gracefully to count 0 (per spec). Contaminant count stays 0/placeholder until someone supplies them.
[chat] SVG stroke set via the `stroke=""` presentation attribute is OVERRIDDEN by any stylesheet `stroke:` rule (presentation attrs have lower priority than CSS). To recolor a path that already has a CSS stroke rule, set it via inline `style="stroke: ..."` (style attr beats stylesheet). Used for the tracker arc threshold colors / blue gradient (url(#cf-arc-grad)).
[chat] There are TWO distinct [role="tablist"] groups in the Recharge Affinity portal that must be styled separately: (1) the PERSISTENT TOP NAV (Dashboard/My Products/Upcoming orders/…) whose tabs carry data-testid^="portal-nav-item" + class recharge-tab-navigation-item — this gets the GLOBAL pill style (border-radius 60px, white/#D9E8F4-active) from recharge-affinity-homepage.liquid and must look IDENTICAL on every route; (2) in-content STATUS FILTERS like Active/Inactive on /subscriptions which use bottom-border underline style. A page-scoped override (body.cf-page-subscriptions ... [role="tablist"]) will blanket-hit BOTH and break the top nav. Always exclude the nav: `[role="tablist"]:not(:has([data-testid^="portal-nav-item"])):not(:has(.recharge-tab-navigation-item))`. Active/Inactive design colors: active = #2C70BB underline + #202635 text, inactive = #797D86 text+underline.
[chat] Portal page background #F2F7FB (Figma "Background/Clear Blue 5%") must be set explicitly on `main#MainContent:has(.recharge-theme)` AND `.recharge-theme`, NOT only via Recharge's --recharge-app-background/--recharge-views-background CSS vars — those vars only paint inner view containers and leave white gaps on short pages / some routes. Cards stay white via their own `.recharge-card { background:#FFFFFF !important }`.
